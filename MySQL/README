The folder contains samples and scripts for the specific vendor's implementation.

Note that MySQL doesn't support INTERSECT or EXCEPT, so Listings 4.001 and 4.003 are missing.

MySQL also does not support returning a 'Table' from a Function, so Listings 4.013 and 4.016 are missing.