The folder contains samples and scripts for the specific vendor's implementation.

REMINDER:
In DB2, use CREATE SCHEMA to create a new container for application tables.  Use
CREATE DATABASE (which in the SQL standard creates a table container) to create
a new instance of the DB2 system.